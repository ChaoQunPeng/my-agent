1、生成代码同时要生成注释，例如关键逻辑，函数都要有注释和业务说明。
2、每次先不改代码，先读取代码逻辑，像用户确认，等确认后，在进行修改。
3、信任项目已有的数据结构、类型定义和调用契约，不要为理论上可能出现的 null、undefined 或异常数据添加防御性判断。
4、只有当类型定义、接口文档、现有代码或已知缺陷明确表明字段可能为空时，才增加非空判断。
5、禁止重复使用 `!== undefined && !== null`。需要空值回退时，优先使用可选链 `?.` 和空值合并 `??`。或者可以查看项目中是否有$isEmpty或者$isNoEmpty或者$hasValue等方法，用于判断为空、有值的情况
6、必要的兜底逻辑必须说明对应的真实业务场景。
7、代码以满足当前明确需求为准，不处理需求之外的假设性边界情况，避免过度设计和过度防御。
