---
trigger: always_on
---

# 前端/后端都需遵循的规则

1. 生成代码的时候必须有注释
2. 禁止修改用户已经修改了的代码,在用户修改的代码基础上进行修改

### 前端

1. 在没有明确要求的情况下,不要修改既有的样式

### 后端

1. 接口使用 RPC 风格，使用 POST 请求，请求体为 JSON 格式，返回体为 JSON 格式
2. 接口地址使用 kebab-case 命名
3. 接口返回值都要使用 ApiResponseDto 封装